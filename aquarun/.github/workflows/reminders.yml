name: Workout reminders

on:
  schedule:
    - cron: '0,30 * * * *'
  workflow_dispatch:

jobs:
  send-reminders:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel reminder endpoint
        run: |
          curl -s -f -H "X-Cron-Secret: ${{ secrets.CRON_SECRET }}" \
            "https://aquarun-omega.vercel.app/api/send-reminders"